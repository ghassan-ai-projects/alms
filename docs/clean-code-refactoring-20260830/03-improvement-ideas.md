# Improvement Ideas

This document captures worthwhile features or engineering improvements noticed during the refactoring. It is intentionally non-implementation scope: ideas are recorded here and are not created as part of this task.

| ID | Area | Idea | Why it may matter | Evidence/status |
|---|---|---|---|---|
| IMP-001 | Mock infrastructure | Add deep-copy helpers for returned models so test assertions cannot mutate mock state through aliased slices or raw JSON. | Reduces false confidence from test-only state aliasing. | Proposal from storemock review; out of scope. |
| IMP-002 | Mock infrastructure | Add explicit cancellation behavior or document that mock context arguments are intentionally ignored. | Makes cancellation coverage and limitations visible. | Proposal from storemock review; out of scope. |
| IMP-003 | Sync semantics | Define deterministic tie-breaking for equal timestamps and consider a timestamp/ID cursor tuple. | Prevents unstable limited results and cursor gaps. | Proposal from storemock review; out of scope. |
| IMP-004 | Mock infrastructure | Add compile-time assertions that mock stores satisfy service store interfaces. | Detects interface drift early. | Proposal from storemock review; out of scope. |
| IMP-005 | Matching | Replace or document the ASCII-only case-insensitive matcher. | Clarifies Unicode behavior in search fixtures. | Proposal from storemock review; out of scope. |
| IMP-006 | Transport validation | Replace ignored request type assertions with explicit malformed-input errors. | Makes invalid MCP payload behavior diagnosable. | Proposal from server review; out of scope. |
| IMP-007 | Learning store response | Decide whether enrichment retrieval failure after `learning.store` should fail the request or return a degraded response. | Avoids silently returning incomplete enrichment state. | Proposal from server review; out of scope. |
| IMP-008 | Health | Inject the reported version and expose a safe structured failure reason or metric. | Removes hardcoded release data and improves operability. | Proposal from server review; out of scope. |
| IMP-009 | Agent update | Define whether an explicit empty display name should clear the field. | Makes update semantics unambiguous. | Proposal from server review; out of scope. |
| IMP-010 | Sync semantics | Add a deterministic timestamp/ID tie-breaker or cursor tuple for learning sync. | Prevents equal-timestamp ordering and cursor gaps. | Proposal from learning-store review; out of scope. |
| IMP-011 | Sync acknowledgements | Derive the cursor from the newest acknowledged record and consider batching acknowledgement inserts. | Avoids caller-order regressions and reduces database round trips. | Proposal from learning-store review; out of scope. |
| IMP-012 | Persistence migrations | Consolidate or correct the migration layout and documented migration command, including enrichment metadata. | Keeps fresh installations aligned with the queries in the store. | Proposal from learning-store review; out of scope. |
| IMP-013 | JSON persistence | Use a nullable-safe JSONB merge for legacy rows. | Prevents `NULL || patch` from discarding enrichment updates. | Proposal from learning-store review; out of scope. |
| IMP-014 | OKF paths | Validate or sanitize learning IDs before constructing bundle paths. | Prevents unsafe path segments in generated bundles. | Proposal from OKF review; out of scope. |
| IMP-015 | OKF descriptions | Make description truncation UTF-8 safe. | Avoids splitting multibyte characters. | Proposal from OKF review; out of scope. |
| IMP-016 | OKF index | Pass structured titles to index rendering instead of reparsing Markdown, and escape generated Markdown metadata. | Reduces format coupling and malformed output risk. | Proposal from OKF review; out of scope. |
| IMP-017 | OKF determinism | Inject the generation clock for deterministic bundle output. | Improves reproducible exports and testability. | Proposal from OKF review; out of scope. |
| IMP-018 | OKF diagnostics | Decide whether malformed enrichment metadata should remain silent or become diagnosable. | Makes export data-quality failures visible. | Proposal from OKF review; out of scope. |
| IMP-019 | Agent pagination | Add deterministic pagination ordering, such as `created_at, agent_id`. | Prevents unstable page boundaries for equal timestamps. | Proposal from agent-store review; out of scope. |
| IMP-020 | Agent JSON | Decide whether list decoding failures should fail the request and share strict/lenient decode policy explicitly. | Avoids silent data loss while preserving deliberate compatibility. | Proposal from agent-store review; out of scope. |
| IMP-021 | Agent API | Consider a typed agent filter and service-boundary validation for IDs and payload sizes. | Makes supported filters and resource limits explicit. | Proposal from agent-store review; out of scope. |
| IMP-022 | Protocol sync | Use a deterministic `(created_at, protocol_id)` cursor and decide whether `PullSince` needs snapshot semantics. | Avoids equal-timestamp gaps and clarifies concurrency behavior. | Proposal from protocol-store review; out of scope. |
| IMP-023 | Protocol lifecycle | Add explicit protocol deactivation/update/delete operations and tag normalization if lifecycle management requires them. | Makes protocol ownership and targeting rules explicit. | Proposal from protocol-store review; out of scope. |
| IMP-024 | Learning lifecycle | Make creation and supersession transactional. | Prevents a stored new learning from remaining when supersession fails. | Proposal from learning-service review; out of scope. |
| IMP-025 | Time | Inject a clock for learning creation and decay. | Enables deterministic time-based behavior. | Proposal from learning-service review; out of scope. |
| IMP-026 | Enrichment | Make enrichment update and top-level score synchronization atomic, and validate patches are JSON objects. | Prevents partial metadata/score state and malformed merges. | Proposal from learning-service review; out of scope. |
| IMP-027 | Deduplication | Replace best-effort bounded searches with database-backed uniqueness and persist a canonical content digest. | Reduces race-prone false negatives and boundary collisions. | Proposal from learning-service review; out of scope. |
| IMP-028 | Service boundaries | Consider separate learning and protocol service types if the combined service expands. | Keeps each domain service focused. | Proposal from learning-service review; out of scope. |
| IMP-029 | GC lifecycle | Validate/normalize intervals and make shutdown idempotent, including an explicit restart policy. | Avoids ticker panics and closed-channel restart failures. | Proposal from GC review; out of scope. |
| IMP-030 | GC correctness | Add a store operation explicitly intended to list all active learnings instead of using an empty full-text query. | Prevents a production GC no-op caused by search semantics. | Evidence gap from GC review; out of scope. |
| IMP-031 | GC scale | Add pagination/cursors beyond 10,000 records and audit/metrics for soft deletion and partial failures. | Makes maintenance complete and observable at scale. | Proposal from GC review; out of scope. |
| IMP-032 | Decay | Share decay arithmetic with scoring while preserving GC best-effort error policy. | Prevents policy drift between maintenance paths. | Proposal from GC review; out of scope. |
| IMP-033 | Deduplication | Persist a canonical content digest and define delimiter/canonicalization rules for atomic database-backed exact deduplication. | Eliminates bounded-search false negatives and hash boundary collisions. | Proposal from dedup review; out of scope. |
| IMP-034 | Deduplication | Make similarity Unicode-aware, impose input/resource limits, and add deterministic tie-breaking. | Makes matching safer and reproducible. | Proposal from dedup review; out of scope. |
| IMP-035 | Deduplication | Align mock and PostgreSQL empty-search semantics and define supersession invariants/transactionality. | Prevents tests from masking production behavior gaps. | Proposal from dedup review; out of scope. |
| IMP-036 | Observability | Add deduplication metrics for bounded searches, false-negative limits, and rejected duplicates. | Makes dedup effectiveness measurable. | Proposal from dedup review; out of scope. |
| IMP-037 | Scoring time | Inject a clock into score decay calculations. | Makes time-based behavior deterministic and easier to verify. | Proposal from scoring review; out of scope. |
| IMP-038 | Score consistency | Use atomic or optimistic-concurrency score updates and enforce database-side bounds. | Prevents lost updates when sync, decay, and enrichment updates race. | Proposal from scoring review; out of scope. |
| IMP-039 | Score validation | Reject NaN, infinity, and invalid negative decrement inputs at the service boundary. | Prevents non-finite or surprising scores from entering persistence. | Proposal from scoring review; out of scope. |
| IMP-040 | GC/scoring scale | Replace the fixed 10,000-record decay scan with a paginated maintenance operation and explicit changed/skipped metrics. | Makes maintenance complete and its result meaningful at scale. | Proposal from scoring review; out of scope. |
| IMP-041 | Resource scale | Add pagination or an explicit bounded response contract to protocol and learning resources. | Prevents large resource reads from becoming expensive or incomplete. | Proposal from resources review; out of scope. |
| IMP-042 | Health | Inject the release version and expose safe failure diagnostics or metrics for degraded agent-count reads. | Makes health output operationally useful without leaking internals. | Proposal from resources review; out of scope. |
| IMP-043 | Tool catalog | Generate or validate the resource catalog against registered tools. | Prevents the manually maintained catalog from drifting from the actual MCP surface. | Proposal from resources review; out of scope. |
| IMP-044 | Resource authorization | Define per-resource authorization and data-minimization policy for aggregate records. | Reduces accidental disclosure through broad resource reads. | Proposal from resources review; out of scope. |
| IMP-045 | Sync acknowledgments | Define atomicity and retry semantics for gap validation, acknowledgement persistence, and cursor advancement. | Prevents partial acknowledgment state under concurrent or failed operations. | Proposal from sync review; out of scope. |
| IMP-046 | Sync cursors | Add deterministic timestamp/ID cursor semantics for learning and protocol pulls. | Prevents equal-timestamp gaps and unstable incremental pulls. | Proposal from sync review; out of scope. |
| IMP-047 | Interface boundaries | Narrow store interfaces by service responsibility after characterization coverage exists. | Reduces coupling while making indirect Learning and Dedup dependencies explicit. | Proposal from interface review; out of scope. |
| IMP-048 | Configuration diagnostics | Decide whether unreadable or malformed configuration files should be reported distinctly. | Improves startup diagnosis without changing fallback policy implicitly. | Proposal from config review; out of scope. |
| IMP-049 | Configuration validation | Add explicit validation for required DSN, server address, and security-sensitive settings. | Prevents invalid runtime configuration from reaching startup. | Proposal from config review; out of scope. |
| IMP-050 | Application startup | Replace the misleading `--migrate` flag behavior with actual migration execution or rename the flag. | Aligns operator expectations with the command’s effect. | Proposal from main review; out of scope. |
| IMP-051 | Process errors | Replace `os.Exit` from the server goroutine with an explicit error handoff and coordinated shutdown. | Makes cleanup and failure propagation more reliable. | Proposal from main review; out of scope. |
